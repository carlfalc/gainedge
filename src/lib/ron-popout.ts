// Legacy RON popout helper — kept as a no-op so existing call sites compile.
export function openRonPopout(_context: Record<string, string | undefined> = {}) {
  /* RON popout removed in Falconer v7 wipe */
}